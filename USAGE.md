# Subtitle Hider 使用指南

## 简介

Subtitle Hider 是一个浏览器扩展，通过隐藏视频字幕帮助你练习英语听力。它会在视频底部创建一个可自定义的遮罩层来遮挡字幕。

## 安装

1. 下载或克隆此项目到本地
2. 打开 Chrome 浏览器，访问 `chrome://extensions/`
3. 开启右上角的「开发者模式」
4. 点击「加载已解压的扩展程序」
5. 选择 `subtitle-hider` 文件夹
6. 安装完成！

## 基本使用

### 启用/禁用字幕隐藏

有两种方式可以切换：

1. **点击扩展图标**
   - 点击浏览器工具栏中的 Subtitle Hider 图标
   - 在弹出窗口中点击「Enable/Disable」按钮
   - 状态指示灯：
     - 🟢 绿色 = 已启用，字幕被隐藏
     - ⚪ 灰色 = 已禁用，字幕可见

2. **键盘快捷键**
   - Windows/Linux: `Ctrl + Shift + S`
   - Mac: `Command + Shift + S`

## 高级功能

### 调整遮罩位置

遮罩层可以自由拖动到屏幕上的任意位置：

1. 将鼠标移动到遮罩层上
2. 按住鼠标左键并拖动
3. 松开鼠标后，位置会自动保存

### 调整遮罩大小

有三种方式可以调整遮罩大小：

#### 方式一：拖动手柄
1. 将鼠标移动到遮罩层右下角
2. 按住调整手柄（会出现斜角光标）
3. 拖动到合适大小后松开

#### 方式二：鼠标滚轮
- **普通滚轮**：同时调整宽度和高度（保持比例）
- **Shift + 滚轮**：仅调整宽度
- **Alt/Command + 滚轮**：仅调整高度

每次滚动调整约 10px，最小宽度 100px，最小高度 30px。

### 设置自动保存

遮罩的位置和大小会自动保存到浏览器存储中。下次启用时会恢复到上次的位置和大小。

## 全屏模式支持

扩展支持多种全屏模式：

### 浏览器原生全屏
按 `F11` 或点击视频的全屏按钮，遮罩会自动移动到全屏区域内。

### Bilibili 网页全屏
在 Bilibili 上，支持以下模式：
- 全屏模式（`data-screen="full"`）
- 网页全屏模式（`data-screen="web"`）

遮罩会自动检测并适应这些全屏状态。

## 支持的视频网站

- YouTube
- Netflix
- Bilibili
- 优酷 (Youku)
- 爱奇艺 (iQiyi)
- 腾讯视频 (Tencent Video)
- Coursera
- Udemy
- Khan Academy
- 其他任何有字幕的视频网站（通用遮罩）

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + Shift + S` | 切换字幕隐藏开关 |
| 鼠标拖动 | 移动遮罩位置 |
| 滚轮 | 调整遮罩大小（整体） |
| `Shift + 滚轮` | 调整遮罩宽度 |
| `Alt/Cmd + 滚轮` | 调整遮罩高度 |

## 注意事项

1. **受限页面**：扩展无法在 `chrome://`、`edge://` 等 Chrome 内置页面上使用
2. **权限要求**：首次使用时需要在普通网页上激活扩展
3. **手动调整**：不同网站的字幕位置不同，可能需要手动调整遮罩位置
4. **全屏兼容**：某些网站可能不支持全屏时的遮罩移动

## 故障排除

### 遮罩没有显示

1. 确认扩展状态为「On」（绿色）
2. 尝试刷新页面
3. 检查是否为受限页面（chrome:// 等）
4. 查看 Chrome 控制台是否有错误信息

### 遮罩位置不对

1. 手动拖动遮罩到正确位置
2. 位置会自动保存
3. 如果进入/退出全屏后位置错误，请手动重新调整

### 快捷键不生效

1. 检查是否被其他扩展占用
2. 在 `chrome://extensions/shortcuts` 中修改快捷键
3. 确保当前页面不是受限页面

### 字幕仍然可见

1. 调整遮罩大小以完全覆盖字幕区域
2. 移动遮罩到字幕实际位置
3. 某些网站的字幕位置特殊，需要仔细调整

## 开发信息

### 项目结构

```
subtitle-hider/
├── manifest.json       # 扩展配置文件
├── content.js          # 核心功能逻辑（遮罩创建、交互）
├── background.js       # 后台服务脚本
├── popup.html          # 弹出窗口界面
├── popup.js            # 弹出窗口逻辑
├── popup.css           # 弹出窗口样式
├── styles.css          # 注入页面的样式
└── icons/              # 扩展图标
```

### 添加新网站支持

编辑 `content.js`，在相关位置添加特定网站的 CSS 选择器。

### 调试

1. 打开 `chrome://extensions/`
2. 开启「开发者模式」
3. 点击扩展卡片中的「背景页」查看后台日志
4. 在网页上按 F12 打开开发者工具查看 content script 日志

## 许可证

MIT License

---

如有问题或建议，欢迎提交 Issue 或 Pull Request！
